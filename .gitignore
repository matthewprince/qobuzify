# dependencies (Qobuzify is zero-dep, but just in case)
node_modules/

# build output / generated
dist/
docs/site.html
site/public/qobuzify.zip

# local Cloudflare state
.wrangler/

# lyric-source credentials (local only, never commit)
.spotify-creds.json
.spotify-user-token.json
.spotify-user-token.json.bak
.apple-creds.json
.musixmatch-creds.json

# dev backups + scratch snapshots
*.bak
*.pre-*
*.qobuzify-bak
.archive/
nul

# private lyrics backend + admin dashboard (kept private, permanently) and local dev scaffolding
server/
dash/
tools/

# internal reverse-engineering research (Qobuz APK teardown) - kept local, not published
docs/qobuz-beta-feature-teardown.md

# native Android app: separate deliverable + build artifacts; not part of the desktop/wrapper release
android/

# logs / os cruft
*.log
.DS_Store
Thumbs.db

build-cache/

# bundled mpv is fetched in CI, never committed (210MB)
wrapper/resources/
